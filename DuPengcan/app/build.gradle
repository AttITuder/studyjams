apply plugin: 'com.android.application'

android {
    signingConfigs {
        keystore {
            keyAlias 'CN'
            keyPassword 'hqdpc0321'
            storeFile file('/media/disk_1/project/study/studyjams/key/keystore.jks')
            storePassword 'hqdpc0321'
        }
    }
    compileSdkVersion 23
    buildToolsVersion "23.0.3"
    defaultConfig {
        applicationId "cn.studyjams.s1.sj38.dupengcan"
        minSdkVersion 21
        targetSdkVersion 23
        versionCode 1
        versionName "1.0"
    }

    lintOptions {
        abortOnError false
    }

    buildTypes {
        release {
            //release的时候加入我么的签名配置
            signingConfig signingConfigs.keystore
            //默认为false，混淆改为true
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    testCompile 'junit:junit:4.12'
    compile 'com.android.support:appcompat-v7:23.1.1'
    compile 'com.inkapplications.viewpageindicator:library:2.4.3'
}
